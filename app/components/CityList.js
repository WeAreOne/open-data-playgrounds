import React, {Component, StyleSheet, View, Image, Text, Dimensions, TouchableOpacity} from 'react-native';
import {Actions} from 'react-native-router-flux'

export default class CityList extends Component {
    render() {
        return (
            <Text>City List</Text>
        )
    }
}